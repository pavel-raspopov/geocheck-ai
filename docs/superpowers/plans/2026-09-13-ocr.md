# 04 OCR (Tesseract.js) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `src/pipeline/ocr.ts` — `recognizeLabels(image: RawImage): Promise<Label[]>` — строго одиночные латинские буквы A–Z, автоприведение к UPPERCASE, координаты центра буквы (ТЗ §3.1), полностью оффлайн (локальный tessdata + локальное WASM-ядро).

**Architecture:** Стадия 3 пайплайна, паттерн `lines.ts`: чистая async-функция над `RawImage`, ленивый `await import('tesseract.js')`, воркер кэшируется на модуль (аналог `cvPromise`). RawImage RGBA кодируется в несжатый 24-битный BMP чистой функцией (v7 `loadImage` не принимает сырые пиксели; BMP-магия явно поддерживается `setImage`). Результат OCR пост-фильтруется чистой функцией (LSTM игнорирует `tessedit_char_whitelist`, поэтому строгий `[A-Z]`-фильтр — в нашем коде).

**Tech Stack:** tesseract.js 7.0.0 (уже в deps), tesseract.js-core 7.0.0, Vitest 5, TypeScript strict.

## Global Constraints

- Vanilla TS + Vite SPA only; логика пайплайна = чистые функции в `src/pipeline/`, без DOM/IO (передача конфиг-строк воркеру допустима; кодирование пикселей — над обычными данными).
- TypeScript strict: `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`; без `any`; границы модулей типизированы.
- Все константы живут в `src/pipeline/constants.ts`, никогда инлайн. OCR-константы **не из ТЗ** (соответствующая пометка): `OCR_LANG`, `OCR_CHAR_WHITELIST`, `OCR_MIN_CONFIDENCE`, `OCR_PSM`, `OCR_USER_DPI`.
- ТЗ §3.1 дословно: «Распознавание текста: Строго одиночные латинские буквы (A-Z). Автоприведение к UPPERCASE. Сохранение координат центра буквы.»
- Оффлайн-bundle (расставлен в спайке): `public/tessdata/eng.traineddata.gz` (2,952,873 байта, `@tesseract.js-data/eng/4.0.0_best_int`) и `public/tesseract/` = `worker.min.js` + `tesseract-core-{lstm,simd-lstm,relaxedsimd-lstm}.{wasm,wasm.js}` (LSTM-only варианты; браузерный `getCore` берёт `tesseract-core-<simd-cap>-lstm.wasm.js` из каталога `corePath`).
- Факты tesseract.js v7, проверенные по `node_modules`: `createWorker(langs='eng', oem=OEM.LSTM_ONLY, options)`; в Node ядро/воркер резолвятся локально из `node_modules` (**не** задавать `workerPath`/`corePath` в Node); `langPath` — файловый путь в Node и URL в браузере (`${langPath}/eng.traineddata.gz` при истинном `gzip`); `cacheMethod: 'none'` отключает кэш-IO; дефолтный output `recognize` — только `{ text: true }`, явно запрашивать `{ blocks: true, text: false }`.
- Vitest UMD/CJS-гоча (observation #5): если `import('tesseract.js')` падает под Vitest — добавить `'tesseract.js'` в `test.server.deps.inline` (внутри ключа `test` в `vitest.config.ts`).
- PowerShell: гейты гейтить как `cmd /c "pnpm.cmd test && echo PASS || echo FAIL"` (observation #2); git add/commit не батчить параллельными вызовами (observation #4).
- Один коммит по завершении: `feat(pipeline): OCR stage (tesseract.js A-Z labels)` — subject only, одна строка. План + ассеты + апдейты трекера/memory — тем же коммитом.
- Подключение UI остаётся в Фазе 4 (06); у этой фичи нет UI-поверхности, визуального шага нет.

## File Structure

- Create: `src/pipeline/ocr.ts` — `encodeBmp(image: RawImage): Uint8Array`, `extractLabels(page: OcrPage): Label[]`, `recognizeLabels(image: RawImage, options?: OcrRuntimeOptions): Promise<Label[]>`; внутренние структурные типы `OcrBboxLike`/`OcrSymbolLike`/`OcrWordLike`/`OcrPage` для JSON воркера.
- Create: `src/pipeline/ocr.spec.ts` — юнит-тесты (BMP-кодер, extraction) + один реальный WASM-интеграционный тест.
- Modify: `src/pipeline/constants.ts` — блок OCR-констант.
- Modify (условное): `vitest.config.ts` — `'tesseract.js'` в `test.server.deps.inline`.
- Modify: `context/library-docs.md` (переписать Tesseract-раздел по v7; исправить неверный `createTesseract`), `context/progress-tracker.md`, `context/build-plan.md`, `context/architecture.md`, `memory.md`.
- Static (уже расставлены, коммитятся здесь): `public/tessdata/eng.traineddata.gz`, `public/tesseract/*` (7 файлов, ~20.4 МБ + 3 МБ).

---

### Task 1: Константы + BMP-кодер (pure, TDD)

**Files:**
- Modify: `src/pipeline/constants.ts`
- Create: `src/pipeline/ocr.ts` (в этой задаче — только `encodeBmp` и типы)
- Test: `src/pipeline/ocr.spec.ts`

**Interfaces:**
- Consumes: `RawImage` из `./lines` (уже экспортирован: `{ width, height, data: Uint8ClampedArray }`, RGBA row-major).
- Produces: `encodeBmp(image: RawImage): Uint8Array` — 24bpp несжатый BMP, строки снизу вверх, пиксели BGR, строки паддируются до 4 байт. Константы `OCR_*` экспортируются из `./constants`.

- [ ] **Step 1: Добавить OCR-константы** в `src/pipeline/constants.ts` (после блока CV):

```ts
/* --- Параметры OCR-стадии (не из ТЗ; tesseract.js v7) --- */

/** Языковая модель OCR (eng = латиница). */
export const OCR_LANG = 'eng';

/** Допустимые метки вершин: строго одиночные заглавные латинские буквы (ТЗ §3.1). */
export const OCR_CHAR_WHITELIST = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';

/** Минимальная уверенность распознавания символа, %. */
export const OCR_MIN_CONFIDENCE = 60;

/** Page segmentation mode 11 = «sparse text» для разбросанных меток вершин. */
export const OCR_PSM = '11';

/** DPI-подсказка, чтобы tesseract не отклонял изображение как слишком мелкое. */
export const OCR_USER_DPI = '96';
```

- [ ] **Step 2: Пишем падающие тесты** для `encodeBmp` в `src/pipeline/ocr.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { encodeBmp } from './ocr';
import type { RawImage } from './lines';

/** 2×2 RGBA: пиксель (0,0) чёрный, остальные белые. */
function tinyImage(): RawImage {
  const data = new Uint8ClampedArray([
    0, 0, 0, 255, 255, 255, 255, 255,
    255, 255, 255, 255, 255, 255, 255, 255,
  ]);
  return { width: 2, height: 2, data };
}

function readU16(b: Uint8Array, off: number): number {
  return b[off]! | (b[off + 1]! << 8);
}
function readU32(b: Uint8Array, off: number): number {
  return (readU16(b, off) | (b[off + 2]! << 16) | (b[off + 3]! << 24)) >>> 0;
}
function readI32(b: Uint8Array, off: number): number {
  return readU32(b, off) | 0;
}

describe('encodeBmp', () => {
  it('пишет корректный 24-битный BMP-заголовок', () => {
    const bmp = encodeBmp(tinyImage());
    expect(bmp[0]).toBe(0x42); // 'B'
    expect(bmp[1]).toBe(0x4d); // 'M'
    expect(readU32(bmp, 10)).toBe(54); // offset пиксельных данных (14+40)
    expect(readU32(bmp, 14)).toBe(40); // размер DIB-заголовка
    expect(readI32(bmp, 18)).toBe(2);
    expect(readI32(bmp, 22)).toBe(2);
    expect(readU16(bmp, 26)).toBe(1); // planes
    expect(readU16(bmp, 28)).toBe(24); // bpp
    expect(readU32(bmp, 30)).toBe(0); // compression
    expect(readU32(bmp, 2)).toBe(bmp.length); // заявленный размер = фактический
  });

  it('кодирует пиксели как BGR снизу вверх', () => {
    const bmp = encodeBmp(tinyImage());
    const rowStart = 54;
    expect([bmp[rowStart], bmp[rowStart + 1], bmp[rowStart + 2]]).toEqual([255, 255, 255]);
    // rowSize при ширине 2: 2*3=6 → пад до 8; верхняя строка (с чёрным пикселем) — вторая
    const topRow = 54 + 8;
    expect([bmp[topRow], bmp[topRow + 1], bmp[topRow + 2]]).toEqual([0, 0, 0]);
  });

  it('паддирует строки до кратности 4 байтам (ширина 3)', () => {
    const white = { width: 3, height: 1, data: new Uint8ClampedArray(3 * 1 * 4).fill(255) };
    const bmp = encodeBmp(white);
    expect(bmp.length).toBe(64); // 54 + 10 (3*3+1 пад)
    expect(readU32(bmp, 2)).toBe(64);
    expect(readU32(bmp, 34)).toBe(10); // biSizeImage
  });
});
```

- [ ] **Step 3: Прогнать — убедиться, что красные**

Run: `cmd /c "pnpm.cmd test -- ocr && echo PASS || echo FAIL"`
Expected: FAIL — `encodeBmp` не существует.
- [ ] **Step 4: Минимальная реализация `encodeBmp`** в `src/pipeline/ocr.ts`:

```ts
/** Стадия 3 пайплайна (ТЗ §2): OCR одиночных латинских букв A–Z + центры (ТЗ §3.1). */
import type { RawImage } from './lines';
import type { Label } from './types';

/**
 * Кодирование RawImage (RGBA) в несжатый 24-битный BMP (байты, Uint8Array).
 * tesseract.js v7 не принимает «сырые» пиксели — только закодированные байты
 * изображения; BMP определяется по magic-байтам 'BM' и декодируется в воркере
 * через bmp-js. Строки снизу вверх, каналы BGR, паддинг строк до 4 байт.
 */
export function encodeBmp(image: RawImage): Uint8Array {
  const { width, height, data } = image;
  const rowSize = Math.ceil((width * 3) / 4) * 4;
  const pixelSize = rowSize * height;
  const bmp = new Uint8Array(54 + pixelSize);
  const view = new DataView(bmp.buffer);

  bmp[0] = 0x42; // 'B'
  bmp[1] = 0x4d; // 'M'
  view.setUint32(2, bmp.length, true);
  view.setUint32(10, 54, true);
  view.setUint32(14, 40, true);
  view.setInt32(18, width, true);
  view.setInt32(22, height, true); // положительная высота = снизу вверх
  view.setUint16(26, 1, true);
  view.setUint16(28, 24, true);
  view.setUint32(30, 0, true);
  view.setUint32(34, pixelSize, true);
  view.setInt32(38, 2835, true); // 96 dpi в px/м
  view.setInt32(42, 2835, true);

  for (let y = 0; y < height; y++) {
    const srcRow = (height - 1 - y) * width * 4;
    const dstRow = 54 + y * rowSize;
    for (let x = 0; x < width; x++) {
      const s = srcRow + x * 4;
      const d = dstRow + x * 3;
      bmp[d] = data[s + 2]!; // B
      bmp[d + 1] = data[s + 1]!; // G
      bmp[d + 2] = data[s]!; // R
    }
  }
  return bmp;
}
```

- [ ] **Step 5: Прогнать — зелёные**

Run: `cmd /c "pnpm.cmd test -- ocr && echo PASS || echo FAIL"`
Expected: PASS (3 BMP-теста).

---

### Task 2: Чистая пост-фильтрация `extractLabels` (TDD)

**Files:**
- Modify: `src/pipeline/ocr.ts`
- Test: `src/pipeline/ocr.spec.ts`

**Interfaces:**
- Consumes: структурные типы в духе JSON-дерева `blocks` (`GetJSONText()` v7; форма из `src/index.d.ts`: `blocks[].paragraphs[].lines[].words[].symbols[]`, узлы с `bbox: {x0,y0,x1,y1}`, `text`, `confidence`; `blocks` может быть `null`).
- Produces: `extractLabels(page: OcrPage): Label[]` — детерминированный порядок (cy ↑, затем cx ↑); `OcrPage` и вспомогательные типы экспортируются из `ocr.ts` (нужны Task 3).

- [ ] **Step 1: Пишем падающие тесты** (append в `ocr.spec.ts`):

```ts
import { extractLabels, type OcrPage } from './ocr';

/** Узел-символ в духе GetJSONText(). */
function sym(text: string, confidence: number, x0: number, y0: number, x1: number, y1: number) {
  return { text, confidence, bbox: { x0, y0, x1, y1 } };
}
function pageOf(words: unknown[]): OcrPage {
  return { blocks: [{ paragraphs: [{ lines: [{ words }] }] }] };
}

describe('extractLabels', () => {
  it('берёт центр bbox и приводит к UPPERCASE', () => {
    const labels = extractLabels(pageOf([{ symbols: [sym('a', 90, 10, 20, 30, 40)] }]));
    expect(labels).toEqual([{ char: 'A', cx: 20, cy: 30 }]);
  });

  it('отбрасывает не-буквы, цифры и многосимвольные узлы', () => {
    const labels = extractLabels(
      pageOf([
        {
          symbols: [
            sym('8', 95, 0, 0, 10, 10),
            sym('→', 90, 0, 0, 10, 10),
            sym('AB', 90, 0, 0, 10, 10),
            sym('c', 80, 4, 6, 12, 14),
          ],
        },
      ]),
    );
    expect(labels).toEqual([{ char: 'C', cx: 8, cy: 10 }]);
  });

  it('отбрасывает символы ниже порога уверенности', () => {
    const labels = extractLabels(
      pageOf([{ symbols: [sym('X', 59, 0, 0, 10, 10), sym('Y', 60, 0, 0, 10, 10)] }]),
    );
    expect(labels).toEqual([{ char: 'Y', cx: 5, cy: 5 }]);
  });

  it('fallback на word-уровень, когда symbols пуст (LSTM-only core)', () => {
    const labels = extractLabels(
      pageOf([{ text: 'K', confidence: 88, bbox: { x0: 0, y0: 0, x1: 10, y1: 20 }, symbols: [] }]),
    );
    expect(labels).toEqual([{ char: 'K', cx: 5, cy: 10 }]);
  });

  it('возвращает [] для null blocks и сортирует по cy, затем cx', () => {
    expect(extractLabels({ blocks: null })).toEqual([]);
    const labels = extractLabels(
      pageOf([
        { symbols: [sym('B', 90, 0, 20, 10, 30), sym('A', 90, 0, 0, 10, 10)] },
        { symbols: [sym('C', 90, 40, 0, 50, 10)] },
      ]),
    );
    expect(labels.map((l) => l.char)).toEqual(['A', 'C', 'B']);
  });
});
```

- [ ] **Step 2: Прогнать — красные**

Run: `cmd /c "pnpm.cmd test -- ocr && echo PASS || echo FAIL"`
Expected: FAIL — `extractLabels` не существует.

- [ ] **Step 3: Минимальная реализация** (append в `ocr.ts`):
- [ ] **Step 3: Минимальная реализация** (append в `ocr.ts`):

```ts
import { OCR_MIN_CONFIDENCE } from './constants';

/** Минимальная структурная модель JSON-дерева blocks (GetJSONText(), tesseract.js v7). */
export interface OcrBboxLike {
  x0: number;
  y0: number;
  x1: number;
  y1: number;
}
export interface OcrSymbolLike {
  text: string;
  confidence: number;
  bbox: OcrBboxLike;
}
export interface OcrWordLike extends OcrSymbolLike {
  symbols?: OcrSymbolLike[];
}
export interface OcrPage {
  blocks: { paragraphs: { lines: { words: OcrWordLike[] }[] }[] }[] | null;
}

const isUppercaseLatin = (text: string): boolean =>
  text.length === 1 && text >= 'A' && text <= 'Z';

/** Проход по дереву: символы с приоритетом, fallback — весь word как одиночная буква. */
function* collectCandidates(
  page: OcrPage,
): Generator<{ char: string; confidence: number; bbox: OcrBboxLike }> {
  for (const block of page.blocks ?? []) {
    for (const paragraph of block.paragraphs) {
      for (const line of paragraph.lines) {
        for (const word of line.words) {
          const symbols = word.symbols ?? [];
          if (symbols.length > 0) {
            yield* symbols;
          } else {
            yield word;
          }
        }
      }
    }
  }
}

/**
 * Пост-фильтрация OCR (ТЗ §3.1): строго одиночные латинские буквы,
 * автоприведение к UPPERCASE (строчные → заглавные), центр = середина bbox.
 * LSTM-движок игнорирует tessedit_char_whitelist, поэтому фильтр здесь обязателен.
 */
export function extractLabels(page: OcrPage): Label[] {
  const labels: Label[] = [];
  for (const { text, confidence, bbox } of collectCandidates(page)) {
    const char = text.trim().toUpperCase();
    if (!isUppercaseLatin(char) || confidence < OCR_MIN_CONFIDENCE) continue;
    labels.push({ char, cx: (bbox.x0 + bbox.x1) / 2, cy: (bbox.y0 + bbox.y1) / 2 });
  }
  // Детерминированный порядок: сверху вниз, слева направо.
  return labels.sort((a, b) => a.cy - b.cy || a.cx - b.cx);
}
```

- [ ] **Step 4: Прогнать — зелёные**

Run: `cmd /c "pnpm.cmd test -- ocr && echo PASS || echo FAIL"`
Expected: PASS (3 BMP + 5 extraction).

---
---

### Task 3: `recognizeLabels` — реальная интеграция воркера (TDD)

**Files:**
- Modify: `src/pipeline/ocr.ts`
- Modify (условное): `vitest.config.ts`
- Test: `src/pipeline/ocr.spec.ts`

**Interfaces:**
- Consumes: `encodeBmp`, `extractLabels`, константы `OCR_*`, `RawImage`.
- Produces: `recognizeLabels(image: RawImage, options?: OcrRuntimeOptions): Promise<Label[]>`; `export interface OcrRuntimeOptions { langPath?: string; workerPath?: string; corePath?: string }` — переопределения путей; в Node дефолт НЕ задаёт worker/core (локальны из node_modules), только `langPath: 'public/tessdata'`; в браузере `'/tessdata'`, `'/tesseract/worker.min.js'`, `'/tesseract'`.

- [ ] **Step 1: Пишем падающий интеграционный тест** (append в `ocr.spec.ts`). Буквы рендерятся без DOM из пиксельного шрифта 5×7, масштаб ×8 (штрих 8 px):

```ts
import { recognizeLabels } from './ocr';

const FONT_5X7: Record<string, string[]> = {
  A: ['.###.', '#...#', '#...#', '#####', '#...#', '#...#', '#...#'],
  B: ['####.', '#...#', '#...#', '####.', '#...#', '#...#', '####.'],
  M: ['#...#', '##.##', '#.#.#', '#...#', '#...#', '#...#', '#...#'],
};

/** Белый холст 340×120; буквы A, B, M с шагом 90 px, глиф 40×56. */
function drawingWithLetters(): RawImage {
  const width = 340;
  const height = 120;
  const data = new Uint8ClampedArray(width * height * 4).fill(255);
  const setBlack = (x: number, y: number): void => {
    const i = (y * width + x) * 4;
    data[i] = 0;
    data[i + 1] = 0;
    data[i + 2] = 0;
  };
  ['A', 'B', 'M'].forEach((ch, index) => {
    const originX = 30 + index * 90;
    const originY = 32; // (120 − 56) / 2
    FONT_5X7[ch]!.forEach((row, ry) => {
      [...row].forEach((cell, rx) => {
        if (cell !== '#') return;
        for (let dy = 0; dy < 8; dy++) {
          for (let dx = 0; dx < 8; dx++) setBlack(originX + rx * 8 + dx, originY + ry * 8 + dy);
        }
      });
    });
  });
  return { width, height, data };
}

describe('recognizeLabels (интеграция, WASM)', () => {
  it('распознаёт A, B, M на синтетическом чертеже', async () => {
    const labels = await recognizeLabels(drawingWithLetters());
    expect(labels.map((l) => l.char).sort()).toEqual(['A', 'B', 'M']);
    const byChar = Object.fromEntries(labels.map((l) => [l.char, l]));
    expect(Math.abs(byChar['A']!.cx - 50)).toBeLessThanOrEqual(10);
    expect(Math.abs(byChar['A']!.cy - 60)).toBeLessThanOrEqual(10);
    expect(Math.abs(byChar['B']!.cx - 140)).toBeLessThanOrEqual(10);
    expect(Math.abs(byChar['M']!.cx - 230)).toBeLessThanOrEqual(10);
  });
});
```

- [ ] **Step 2: Прогнать — красные**

Run: `cmd /c "pnpm.cmd test -- ocr && echo PASS || echo FAIL"`
Expected: FAIL — `recognizeLabels` не существует. Если импорт tesseract.js падает с `TypeError … incompatible receiver` — добавить `'tesseract.js'` в `test.server.deps.inline` в `vitest.config.ts` (строго под ключом `test`) и повторить.

- [ ] **Step 3: Реализация `recognizeLabels`** (append в `ocr.ts`):

```ts
/** Переопределяемые пути (браузер — статика из public/; Vitest/Node — node_modules + локальный tessdata). */
export interface OcrRuntimeOptions {
  langPath?: string;
  workerPath?: string;
  corePath?: string;
}

const isNode = typeof process !== 'undefined' && process.versions?.node !== undefined;

/** Локальный bundle без CDN (решение фичи: public/tessdata + public/tesseract). */
function defaultOptions(): OcrRuntimeOptions {
  if (isNode) {
    // Node: воркер и WASM-ядро резолвятся из node_modules; переопределять
    // workerPath/corePath нельзя — сломает node-воркер. Только локальный tessdata.
    return { langPath: 'public/tessdata' };
  }
  return {
    langPath: '/tessdata',
    workerPath: '/tesseract/worker.min.js',
    corePath: '/tesseract',
  };
}

/** Структурный тип воркера — без импорта типов tesseract.js на границе модуля. */
interface TesseractWorkerLike {
  setParameters(params: Record<string, string>): Promise<unknown>;
  recognize(
    image: Uint8Array,
    options?: Record<string, never>,
    output?: { blocks: boolean; text: boolean },
  ): Promise<{ data: OcrPage }>;
}

/** Один воркер на модуль — как cvPromise в lines.ts. */
let workerPromise: Promise<TesseractWorkerLike> | null = null;

async function loadWorker(options: OcrRuntimeOptions): Promise<TesseractWorkerLike> {
  const mod = (await import('tesseract.js')) as unknown as {
    createWorker: (
      langs: string,
      oem?: number,
      opts?: Record<string, unknown>,
    ) => Promise<TesseractWorkerLike>;
  };
  // oem по умолчанию LSTM_ONLY (v7); cacheMethod 'none' — детерминизм без кэш-IO.
  const worker = await mod.createWorker(OCR_LANG, undefined, {
    ...options,
    cacheMethod: 'none',
    gzip: true,
    logger: () => {},
  });
  await worker.setParameters({
    tessedit_pageseg_mode: OCR_PSM,
    tessedit_char_whitelist: OCR_CHAR_WHITELIST,
    user_defined_dpi: OCR_USER_DPI,
  });
  return worker;
}

/**
 * OCR-стадия: RawImage → BMP → воркер → метки вершин (ТЗ §3.1).
 * Асинхронность — только из-за WASM-загрузки; DOM не используется.
 */
export async function recognizeLabels(
  image: RawImage,
  overrides?: OcrRuntimeOptions,
): Promise<Label[]> {
  const options = { ...defaultOptions(), ...overrides };
  workerPromise ??= loadWorker(options);
  const worker = await workerPromise;
  // Явный output: дефолт v7 — только text, blocks не построится.
  const { data } = await worker.recognize(encodeBmp(image), undefined, {
    blocks: true,
    text: false,
  });
  return extractLabels(data);
}
```

Заметки к реализации:
- `createWorker(OCR_LANG, undefined, …)` — v7-сигнатура `(langs, oem, options, config)`; `undefined` oem = встроенный дефолт `OEM.LSTM_ONLY`. Если TS-тип `oem?: OEM` не пропустит `undefined` — передать `1` (OEM.LSTM_ONLY) явно.
- Если TS-типы tesseract.js конфликтуют со структурным `TesseractWorkerLike` — приведение через `as unknown as`, как в `lines.ts`.
- Happy-path тест вызывает `recognizeLabels(image)` без overrides; пути по умолчанию уже корректны (cwd Vitest = корень проекта).

- [ ] **Step 4: Прогнать — зелёные**

Run: `cmd /c "pnpm.cmd test -- ocr && echo PASS || echo FAIL"`
Expected: PASS (9 тестов; первый интеграционный прогон может занять 10–60 s — WASM + модель).
Если пиксельный шрифт не распознаётся (блочные буквы) — правится `drawingWithLetters` (масштаб/шаг), это настройка фикс тура, не продакшн-код.

---

### Task 4: Гейты, build, доки, memory, коммит

**Files:**
- Modify: `context/library-docs.md`, `context/progress-tracker.md`, `context/build-plan.md`, `context/architecture.md`, `memory.md`
- Commit: всё, включая `public/`-ассеты и план.

- [ ] **Step 1: Полная лестница верификации** (каждая — отдельная команда, PASS/FAIL по exit-коду):

```
cmd /c "pnpm.cmd format && echo FMT_OK"                       (новые файлы через prettier --write)
cmd /c "pnpm.cmd test && echo PASS || echo FAIL"              → 41/41 (32 + 9 новых)
cmd /c "pnpm.cmd typecheck && echo PASS || echo FAIL"
cmd /c "pnpm.cmd lint && echo PASS || echo FAIL"
cmd /c "pnpm.cmd format:check && echo PASS || echo FAIL"
cmd /c "pnpm.cmd build && echo BUILD_OK"
```

Интеграционный WASM-тест в Vitest (plain Node) уже является smoke реальной стадии; отдельный node-скрипт не нужен.

- [ ] **Step 2: Docs**

- `context/library-docs.md`, раздел Tesseract.js — переписать по v7: `createWorker(langs, oem, options)` (НЕ `createTesseract`); recognize принимает закодированные байты (Uint8Array; BMP распознаётся по magic 'BM' и ре-кодируется через bmp-js); output по умолчанию `{text:true}` → явно `{blocks:true,text:false}` (JSON-дерево `GetJSONText()`); LSTM игнорирует `tessedit_char_whitelist` → пост-фильтр `[A-Z]` в коде; локальный bundle: `langPath` (URL в браузере / путь в Node) + `workerPath`/`corePath` только в браузере; `cacheMethod:'none'`; PSM '11' (sparse); DPI-подсказка `user_defined_dpi`.
- `context/architecture.md`: строку стадии 3 дополнить: «(BMP-кодирование RawImage→BMP 24bpp, PSM 11, пост-фильтр A–Z, воркер кэшируется на модуль)».
- `context/build-plan.md`: пункт 04 → ✅ с датой 2026-09-13 и деталями (BMP-кодер, локальный bundle, 9 тестов).
- `context/progress-tracker.md`: Phase 3 → `[x] 04 OCR` со сводкой; Phase = Phase 3 в работе (05 — следующая).

- [ ] **Step 3: `/remember save`** → `memory.md` (Session 4: что построено/решения — локальный bundle закрывает открытый вопрос/проблемы/состояние/next = 05 graph.ts).

- [ ] **Step 4: Один коммит (subject only), последовательно:**

```bash
git add -A ; git commit -m "feat(pipeline): OCR stage (tesseract.js A-Z labels)"
```

---

## Self-Review

- **Spec coverage:** ТЗ §3.1 (одиночные буквы A–Z, UPPERCASE, центры) → Task 2 (фильтр) + Task 3 (стадия); оффлайн-bundle → Global Constraints + Task 3 (`defaultOptions`); гейты/доки/коммит → Task 4. Сознательный gap: UI-оверлей меток отложен в 06 (build-plan, «wire logic»).
- **Placeholder scan:** нет («TBD/TODO/…» отсутствуют; все шаги с кодом или командами).
- **Type consistency:** `RawImage` из `./lines`; `Label` из `./types`; `OcrPage`/`OcrSymbolLike`/`OcrBboxLike`/`OcrWordLike` определены в Task 2 и потребляются Task 3; `encodeBmp` (Task 1) потребляется Task 3; `OcrRuntimeOptions` определён в Task 3.